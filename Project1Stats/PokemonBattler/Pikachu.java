package PokemonBattler;

public class Pikachu extends Pokemon {      // Pikachu class extends to Pokemon superclass

    public Pikachu() {                      // Pikachu constructor
        setHp(35);                   // sets hp to 35
        setAttack(55);           // sets attack to 55
        setDefense(40);         // sets defense to 40
        setSpAttack(50);       // sets spAttack to 50
        setSpeed(90);             // sets speed to 90
    }
}